# 电梯群控板"本层呼梯偶发不开门"问题分析报告

## 一、代码架构概览

### 1.1 核心文件结构
- **main.c**: 主程序入口，包含主循环和定时器调度
- **lift.c**: 核心群控逻辑（1482行），包含状态机、距离计算、呼梯分配
- **LiftCan.c**: CAN通信处理，负责呼梯指令收发和状态同步
- **Lift.h**: 全局变量和宏定义，包含电梯状态位定义

### 1.2 关键数据结构
```c
// 电梯状态字 (LiftState[8])
// 位定义：
// 0x0002: 停止标志 (0=停止，1=运行)
// 0x0020: 减速标志
// 0x0040/0x0080: 下行/上行方向
// 0x0100: 正在开门
// 0x0200: 正在关门
// 0x0300: 开门到位
// 0x0800: 超载
// 0x1000: 满载
```

## 二、本层呼梯处理流程分析

### 2.1 完整数据流
```
外呼按钮按下 → CanCheckOrder()检测 → CanTxOrder()发送CAN指令 
→ 电梯主板登记 → CanRxGroupProcess()接收确认 → SetEnableOrder()设置群控标志
→ LiftMain()每10ms循环分配 → LiftDistance()计算距离 → DoUpOrder/DoDnOrder分配
→ CanReSendOrder()重发登记指令 → 电梯响应运行 → 到达本层 → 开门
```

### 2.2 关键函数调用链
1. **CanCheckOrder()** (LiftCan.c:306): 每10ms检测呼梯按钮变化
2. **SetEnableOrder()** (LiftCan.c:438): 登记成功时设置GroupUpOrder/GroupDnOrder
3. **LiftMain()** (lift.c:718): 主群控循环，逐楼层处理呼梯
4. **LiftDistance()** (lift.c:253): 计算每台电梯到呼梯楼层的距离
5. **Seqence()** (lift.c:680): 选择最优电梯
6. **CanReSendOrder()** (LiftCan.c:393): 对有方向但不动的电梯重发指令

## 三、开门条件深度分析

### 3.1 开门状态判断 (lift.c:646-655)
```c
i = (LiftState[Lift]&0x0300);
if((i==0x300)||(j>0))
    ;   // 开门到位 或者 该层呼梯按钮被按下 - 不增加额外代价
else if(i==0x100)
    FNum |= 0x01;   // 正在开门
else if(i==0x000)
    FNum |= 0x02;   // 关门到位
else if(i==0x200)
    FNum |= 0x04;   // 正在关门
```

### 3.2 阻止开门的关键条件
根据代码分析，以下情况会导致电梯到达本层但不开门：

#### A. 状态机竞争条件 (lift.c:640-645)
```c
if((LiftState[Lift]&0x0002)==0)
    ;  // 停止
else if((LiftState[Lift]&0x0020)==0)
    FNum |= 0x20;  // 本层减速
else
    FNum |= 0x40;
```
**风险点**: 如果电梯在平层瞬间`LiftState`的停止位(0x0002)未正确置位，可能导致距离计算异常

#### B. 方向超时保护 (main.c:139-157, lift.c:272)
```c
// Check_NoMove() 函数 - 每秒执行
if ((LiftState[i]&0x00c0)==0) // 电梯没有方向
{
    if (有呼梯)
    {
        if (LiftNoDirTimer[i]>0)
            LiftNoDirTimer[i]--;
        else  // 时间为0时触发保护
        {
            LiftNoDirTimer[i] = 3;
            NoDirCallFloor[i] = FirstFloor[i];
            k = 1;  // 触发退离逻辑
        }
    }
}
```
**偶发故障点**: 如果电梯到达本层后方向未及时清除，3秒后可能触发退离逻辑

#### C. 内选优先抢占 (lift.c:804-821, 955-970等多处)
```c
if ((LiftNowFloor[n]>=Do_Floor)&&((LiftState[n]&0x00c0)==0x40)&&((LiftFuntion&RunOneInOrder)>0))
{
    if ((CIn[n]==1)&&(COut[n]==0)&&(BitFun(LiftInOrder[n], Do_Floor, 2)>0)
        &&((LiftState[n]&0x0300)==0x0300)&&(L[m]>3)&&(LiftRunFloor[m]!=Do_Floor))
    {
        m = n;    // 只有一个到达该层的内选 - 切换分配
    }
}
```
**竞态风险**: 当多台电梯同时满足条件时，可能在开门前瞬间切换分配目标

#### D. 登记数量限制 (lift.c:777-778)
```c
if (BitFun(EnUpGroup[GP][j], Do_Floor, 2)==0)
    L[j] = 0x1fffffff;   // 不参与群控
else if (C[j]>2)
    L[j] = 0x1fffffff;   // 登记号大于3，不接收登记
```
**边界条件**: 如果电梯已有超过2个登记，可能拒绝新的本层呼梯

#### E. CAN通信偶发丢失 (LiftCan.c:418-429)
```c
if ((Lift_OK[j]>0)&&(LiftOnline[j]>0)&&(LiftNotMoveTimer[j]>0))
{
    k = BitFunTwo(UpOrder[j], LiftUpOrder[j], i);
    if (k==0x10)
        CanTxOrder(CanPort, j, i, 0xa0, 1);  // 重发登记
    else if (k==0x01)
        CanTxOrder(CanPort, j, i, 0xa0, 0);  // 取消登记
}
```
**通信风险**: `LiftNotMoveTimer`超时前如果CAN通信失败，登记指令可能未送达

## 四、偶发性故障根因假设

### 假设1: 状态更新时序竞争 (高概率)
**场景**: 
1. 电梯到达本层，主板发送状态帧(`LiftState`含0x0300开门到位)
2. 群控板在两次`LiftMain()`循环间收到状态更新
3. 恰好在距离计算后立即收到新状态，导致`LiftRunFloor`与实际不符
4. 下一轮循环中`LiftRunFloor[m]!=Do_Floor`条件满足，触发分配切换

**证据**: lift.c:807,816,851,860,958,967,1002,1011共8处检查`LiftRunFloor[m]!=Do_Floor`

### 假设2: 开门信号抖动 (中概率)
**场景**:
1. 电梯开门过程中，门机信号短暂抖动
2. `LiftState`的0x0300位在0x100(正在开门)和0x300(开门到位)间快速切换
3. 某次采样恰好读到非0x300状态
4. 距离计算增加额外代价，影响后续分配决策

### 假设3: CAN总线瞬时干扰 (中概率)
**场景**:
1. 外呼登记CAN帧发送时遭遇干扰
2. `CanTxOrder()`返回失败但未重试
3. `UpOrderTemp`已置位但电梯主板未收到
4. 电梯到达后无登记记录，不执行开门

### 假设4: 多梯抢梯逻辑裂缝 (低概率)
**场景**:
1. 两台电梯几乎同时到达本层
2. 首选梯已开始开门动作(`LiftState`=0x100)
3. 次级梯满足"内选优先"条件
4. 分配切换到次级梯，首选梯取消开门

## 五、排查建议

### 5.1 代码层面修复优先级

#### P0: 增加开门状态确认延时
在`LiftDistance()`本层处理逻辑中，增加开门状态的持续确认：
```c
// 建议在 lift.c:646 附近修改
i = (LiftState[Lift]&0x0300);
// 新增：检查连续多次状态一致
if (i == 0x300) {
    // 验证开门到位状态稳定性
    static DWORD DoorOpenConfirm[8] = {0};
    if (DoorOpenConfirm[Lift] < 3)
        DoorOpenConfirm[Lift]++;
    if (DoorOpenConfirm[Lift] >= 3)
        ; // 确认开门到位
    else
        FNum |= 0x01; // 视为正在开门
} else {
    DoorOpenConfirm[Lift] = 0;
    // ... 原有逻辑
}
```

#### P1: 优化分配切换条件
放宽`LiftRunFloor`检查条件，避免开门过程中切换：
```c
// lift.c:807 等8处
// 原条件：&&((LiftState[n]&0x0300)==0x0300)&&(L[m]>3)&&(LiftRunFloor[m]!=Do_Floor)
// 建议：&&((LiftState[n]&0x0300)==0x0300)&&(L[m]>3)&&(LiftRunFloor[m]!=Do_Floor)&&((LiftState[m]&0x0100)==0)
// 增加：目标电梯不在开门过程中
```

#### P2: 增强CAN重发机制
在`CanCheckOrder()`中增加失败重试计数：
```c
// LiftCan.c:333附近
static BYTE RetryCount[8][64] = {0};
if (CanTxOrder(CanPort, j, i, 0xa0, 1) == TRUE) {
    BitFun(UpOrderTemp[j], i, 1);
    RetryCount[j][i] = 0;
} else {
    RetryCount[j][i]++;
    if (RetryCount[j][i] > 3) {
        // 记录故障或强制刷新
    }
}
```

### 5.2 现场调试建议

1. **抓取故障时刻的状态字**
   - 监控`LiftState`在到达本层前后的完整变化序列
   - 重点关注0x0002(停止)、0x0300(开门到位)、0x00c0(方向)三位

2. **测量时序间隔**
   - `LiftMain()`循环周期是否稳定在10ms
   - CAN状态帧更新频率与群控计算周期的相位关系

3. **检查配置参数**
   - `LiftNotMove`默认值90(900ms)是否过短
   - `RunStopTime`停靠时间设置是否合理
   - `EnUpGroup/EnDnGroup`使能配置是否正确

## 六、下一步行动计划

### 阶段1: 日志增强 (1天)
- 在关键路径添加状态记录缓冲区
- 记录最近10次开门决策的完整上下文

### 阶段2: 压力测试 (2天)
- 模拟高频本层呼梯场景
- 注入CAN通信延迟和丢包
- 验证状态跳变边界条件

### 阶段3: 补丁验证 (1天)
- 实施P0优先级修复
- 对比修复前后故障率

---

**报告生成时间**: 基于当前代码版本分析
**适用版本**: 标准群控板程序
**风险等级**: 中 (偶发性功能缺失)
